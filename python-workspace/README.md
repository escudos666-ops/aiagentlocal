# Agentics Open Terminal Workspace

This folder is mounted into the `open-terminal` container at `/home/user/workspace`.

Use it for scratch Python files, terminal experiments, and small agent-generated artifacts that should stay outside the main project tree.

Do not put secrets, tokens, cookies, or production data here.
