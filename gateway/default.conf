resolver 127.0.0.11 valid=30s ipv6=off;

map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

server {
    listen 80;
    server_name ai.localhost;

    location / {
        set $upstream open-webui:8080;
        proxy_pass http://$upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name n8n.localhost;

    location / {
        set $upstream n8n:5678;
        proxy_pass http://$upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name db.localhost;

    location / {
        set $upstream adminer:8080;
        proxy_pass http://$upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server {
    listen 80;
    server_name files.localhost;

    location / {
        set $upstream minio:9001;
        proxy_pass http://$upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name graphql.localhost;

    location / {
        set $upstream postgraphile:5000;
        proxy_pass http://$upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server {
    listen 80;
    server_name grafana.localhost;

    location / {
        set $upstream grafana:3000;
        proxy_pass http://$upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name dashboard.localhost;

    location / {
        set $upstream agentics-dashboard:8080;
        proxy_pass http://$upstream;
        proxy_set_header Host $host;
    }
}

server {
    listen 80;
    server_name hf.localhost;

    location / {
        set $upstream agentics-hugginhgface:8082;
        proxy_pass http://$upstream;
        proxy_set_header Host $host;
    }
}

server {
    listen 80;
    server_name pa.localhost;

    location / {
        set $upstream agentics-pa-service:8000;
        proxy_pass http://$upstream;
        proxy_set_header Host $host;
    }
}

server {
    listen 80;
    server_name pytorch.localhost;

    location / {
        set $upstream agentics-pytorch-service:8888;
        proxy_pass http://$upstream;
        proxy_set_header Host $host;
    }
}

server {
    listen 80;
    server_name preview.localhost;

    location / {
        set $upstream agentics-webui_preview:80;
        proxy_pass http://$upstream;
        proxy_set_header Host $host;
    }
}
