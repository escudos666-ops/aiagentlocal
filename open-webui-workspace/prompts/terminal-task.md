Use Agentics Open Terminal as the execution sandbox for this task.

First run `terminal_health`.

Then run only the commands needed to answer or validate the task. Keep commands short, avoid secrets, and use `/home/user/workspace` unless another path is explicitly required.

If a command may take longer than 30 seconds, start it as a background process, keep the process id, poll status, and stop it if it runs away.

Finish with the command results and the next concrete action.
